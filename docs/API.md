# MedVision AI API

## POST /upload/image
Uploads a medical image and returns a URL.

## POST /predict/breast-cancer
Accepts image URL or base64 and returns prediction, confidence, and explainability.

## POST /report/generate
Generates a PDF diagnostic report for a prediction.

## GET /history
Returns scan history for the authenticated user.

## GET /profile
Returns the user profile.
