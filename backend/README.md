# Backend

FastAPI service for MedVision AI, providing prediction, reporting, and history APIs.

## Local Run

1. Install requirements: pip install -r requirements.txt
2. Run: uvicorn main:app --reload
